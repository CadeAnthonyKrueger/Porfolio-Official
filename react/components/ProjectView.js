import React from 'react'; // Make sure to import React

const ProjectView = () => {

    const style = {
        backgroundColor: 'red',
        width: '80%',
        height: '100%',
        color: 'white'
    }

    return (
      <div className="ProjectView">
        Project View - The project view is in the houseeeee
      </div>
    );
}

export default ProjectView;
